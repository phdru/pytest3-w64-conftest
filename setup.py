#!/usr/bin/env python

from setuptools import setup

setup(
    name="pytest3-w64-conftest",
    version='0.0.1',
    description="Test for a problem with pytest3 at w64 and `pytest_addoption()` in `conftest.py`",
)
